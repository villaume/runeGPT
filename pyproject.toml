[project]
name = "runegpt"
version = "0.1.0"
description = "A small transformer trained on runes instead of tokens"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "mlx>=0.21",
    "numpy>=2.0",
]
